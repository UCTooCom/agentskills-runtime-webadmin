# AirJelly 与 AgentSkills Runtime 对比分析

## 产品定位对比

| 维度 | AirJelly | AgentSkills Runtime |
|------|----------|-------------------|
| **定位** | 个人数据源/生活日志 | AI 技能执行环境 |
| **核心功能** | 记忆存储、任务管理、日程、应用使用追踪 | 技能加载、执行、编排 |
| **用户场景** | 个人生产力工具 | 开发者/企业的 AI 能力集成 |
| **数据方向** | 数据生产者（采集用户活动） | 数据处理者（执行技能处理任务） |

## 功能对比

| 功能 | AirJelly | AgentSkills Runtime |
|------|----------|-------------------|
| **本地 API** | ✅ 提供本地 HTTP API | ✅ 提供 HTTP/WebSocket API |
| **数据存储** | ✅ 记忆、任务、日程 | ❌ 不存储数据（依赖外部数据库） |
| **Skill 标准** | ❌ 无 | ✅ SKILL.md 开放标准 |
| **WASM 沙箱** | ❌ 无 | ✅ 基于 WASM 的安全执行 |
| **MCP 集成** | ❌ 无 | ✅ 支持 MCP 协议 |
| **RAG 搜索** | ❌ 无 | ✅ 混合密集+稀疏搜索 |
| **多语言 SDK** | 仅 TypeScript | JS/Python/Java/Go/Rust/C# |
| **技能市场** | ❌ 无 | ✅ 支持技能市场扩展 |
| **权限体系** | 简单 token 认证 | 完整的 RBAC 权限系统 |

## 各自优势

### AirJelly 优势

1. **开箱即用**：Desktop 应用，无需配置直接使用
2. **数据采集**：自动采集应用使用、屏幕录制等个人数据
3. **用户界面**：有完整的 Desktop 客户端
4. **隐私保护**：数据完全本地存储，不上云

### AgentSkills Runtime 优势

1. **标准化**：遵循 AgentSkills 开放标准，可跨平台复用
2. **可扩展**：支持开发者自定义技能，技能市场机制
3. **安全执行**：WASM 沙箱隔离，权限控制完善
4. **多技术栈**：多语言 SDK，无缝集成现有项目
5. **企业友好**：完整权限体系、审计日志、数据库集成
6. **国产自主**：仓颉语言实现，自主可控

## 各自劣势

### AirJelly 劣势

1. **封闭生态**：SDK 仅 TypeScript，难以集成到其他技术栈
2. **依赖桌面**：必须安装运行 Desktop 应用
3. **无技能扩展**：无法自定义新的技能类型
4. **非标准**：私有 API，无开放标准
5. **场景局限**：主要是个人生产力，数据无法被其他应用利用

### AgentSkills Runtime 劣势

1. **无数据源**：不提供记忆、日程等数据采集能力
2. **配置复杂**：需要配置数据库、大模型 API 等
3. **学习成本**：需要理解 Skill 标准才能开发技能
4. **依赖大模型**：需要外部 LLM API

## 关系判断

**两者不是直接竞品，而是互补关系。**

- **AirJelly** 可以作为 **AgentSkills Runtime** 的**数据源技能**
- AirJelly 提供记忆、日程等数据
- AgentSkills Runtime 可以开发一个 `airjelly-skill` 来调用 AirJelly SDK，读取用户记忆并进行处理

```
┌─────────────────────────────────────────────────────┐
│            AgentSkills Runtime                       │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐ │
│  │ airjelly-   │  │ uctoo-api-  │  │ code-gen-   │ │
│  │ skill       │  │ skill       │  │ skill       │ │
│  └──────┬──────┘  └──────┬──────┘  └──────┬──────┘ │
│         │                 │                 │        │
│         ▼                 ▼                 ▼        │
│  ┌─────────────────────────────────────────────────┐ │
│  │              技能执行层 (WASM 沙箱)              │ │
│  └─────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────┘
         │                        ▲
         ▼                        │
┌─────────────────────┐    ┌─────────────────────┐
│    AirJelly SDK     │───▶│   AirJelly Desktop   │
│  (@airjelly/sdk)    │    │   (数据源)            │
└─────────────────────┘    └─────────────────────┘
```

## 总结

| 判断 | 说明 |
|------|------|
| **是否对标？** | 不是直接竞品，定位不同 |
| **AirJelly 是什么？** | 个人数据采集和记忆系统，类似"数字孪生" |
| **AgentSkills Runtime 是什么？** | AI 技能执行平台，类似"AI 技能操作系统" |
| **能否结合？** | ✅ 可以，AgentSkills Runtime 可以调用 AirJelly SDK 作为数据源 |

如果您希望让 AgentSkills Runtime 能够利用 AirJelly 的用户记忆数据，完全可以开发一个 `airjelly-skill` 技能，让 AI 能够查询用户的历史记忆、日程等，实现更个性化的 AI 助手。
